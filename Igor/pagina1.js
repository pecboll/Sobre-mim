var txtn = document.getElementById('lisn')
var txtl = document.getElementById('lisl')

function nomear(el) {
    let disp = document.getElementById('pn').style.display;
    if (disp == "none") {
        let disp = document.getElementById('pn').style.display = 'block'
    } else {
        let disp = document.getElementById('pn').style.display = 'none'
    }
    function datar(el) {
        let disp = document.getElementById('pn').style.display;
        if (disp == "none") {
            let disp = document.getElementById('pn').style.display = 'block'
        } else {
            let disp = document.getElementById('pn').style.display = 'none'
        }
    //let nome = ('Igor Rocha de Azevedo')
    //txtn.innerHTML = `${nome}`

}